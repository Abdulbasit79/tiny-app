<header>
    <nav class="navbar navbar-expand-md navbar-dark bg-success">
        <a class="navbar-brand" href="/urls">TinyApp</a>
        <button
          class="navbar-toggler"
          type="button"
          data-toggle="collapse"
          data-target="#navbarNavAltMarkup"
          aria-controls="navbarNavAltMarkup"
          aria-expanded="false"
          aria-label="Toggle navigation"
        >
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
          <div class="navbar-nav">
            <a class="nav-item nav-link" href="/urls">My URLs</a>
            <a class="nav-item nav-link" href="/urls/new">Create New URL</a>
        </div>
        </div>
        <% if (typeof user !== "undefined"){ %>
          <div>

            <form id="logout" method="POST" action="/logout" style="font-size: 1.05em">
              <span>Logged In As: <%=user.email%></span>
            </form>
          </div>
          <div>
            <button class="btn btn-secondary" form="logout" style="margin-left: 0.7em" >Logout</button>
          </div>
          <% } else{ %>
            <div class="d-flex text-right">
              <form class = "form-inline"id="login" method="GET" action="/login">
                
                <button class="btn btn-secondary" form="login">Login</button>
            
            </form>
            <form class = "form-inline"id ="register" method="GET" action="/register">
                <button class= "btn btn-secondary" type ="submit">Register</button>
            
            </form>
            
        </div>
            <div>
              
            </div>
            <% } %>
        </div>
          </nav>
          </header>
